{
  "name": "clever-vpn-download-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "render-config": "node scripts/render-wrangler.mjs",
    "deploy": "npm run render-config && wrangler deploy --config wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.19.1"
  }
}